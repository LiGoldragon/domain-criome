# domain-criome

Criome domain registry, resolver, and projection runtime.

This repo is intentionally documentation-only at birth. The contracts are live
in `signal-domain-criome` and `owner-signal-domain-criome`; runtime binaries
should land only with a real daemon request path.
